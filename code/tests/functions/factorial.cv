program factorial

function fact (int arg1) returns int [
    int sol = 1
    if (arg1 > 0) [
      sol = fact(arg1-1) * arg1
    ]
    return sol
]

main [
    int myVar = fact(10)
    print(myVar)
]

finish
