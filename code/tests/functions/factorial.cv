program hello_world2

function myfunc1 (int arg1) returns int [
    int sol = 1
    if (arg1 > 0) [
      sol = myfunc1(arg1-1) + arg1
    ]
    circle myCirc (sol, sol)
    return sol
]

main [
    canvas myCanvas
    int myVar = myfunc1(100)
    print(myVar)
    paint myCanvas
]

finish
