program factorial

function fact (int limit) returns int [
    int sol = 1
    int i = 1
    while (i <= limit) [
        sol = sol * i
        i = i + 1
    ]
    return sol
]

main [
    int factorial = fact(10)
    print(factorial)
]

finish
